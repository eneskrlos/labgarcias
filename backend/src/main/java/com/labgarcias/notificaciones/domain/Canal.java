package com.labgarcias.notificaciones.domain;

/** §6.3: lista cerrada de canales de envío (chk_envio_canal, ampliado por V2 con WHATSAPP). */
public enum Canal {
    APP,
    CORREO,
    TELEGRAM,
    WHATSAPP
}
