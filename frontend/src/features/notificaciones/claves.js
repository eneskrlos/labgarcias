/**
 * Claves de TanStack Query de la campana. Viven aparte porque el contador y el panel son
 * componentes distintos que tienen que invalidarse entre sí.
 */
export const CLAVE_CONTADOR = ['notificaciones', 'contador'];
export const CLAVE_LISTADO = ['notificaciones', 'listado'];
