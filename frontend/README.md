# Lab. Garcia's Connect — Frontend

React + Vite. Ver `docs/Agente.md` (sección 5.7) y `docs/spec.md` (sección 8) en la raíz del repo.

## Desarrollo

```
cp .env.example .env   # ajustar VITE_API_BASE_URL si hace falta
npm install
npm run dev
```
